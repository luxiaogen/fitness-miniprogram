.empty {
  padding: 100rpx 0;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.icon { font-size: 80rpx; opacity: 0.45; margin-bottom: 20rpx; }
.text { font-size: 26rpx; color: var(--on-surface-variant); }
.sub { font-size: 24rpx; color: var(--on-surface-muted); margin-top: 8rpx; }
